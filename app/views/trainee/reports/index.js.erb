$("#list-reports").html("<%= j render partial: "trainee/reports/report",
  collection: @reports %>").fadeOut(100, function() {
    $("#list-reports").fadeIn(100);
  });
$(".image-report img").addClass("size-img-report");
$("#paginate-reports").html("<%= j paginate(@reports, remote: true) %>");
$(".flash-report").html("");
selectable.update();
